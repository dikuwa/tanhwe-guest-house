# Component plan

Reused: Button, Input, Textarea, Label, Select, Badge, Card, SiteHeader, SiteFooter, ContactActions, AvailabilitySearch, BookingRequestForm, RoomCard, AdminNav, RoomForm, BookingForm, BookingStatus, and ImageUploader.

Added: SiteNavigation, CustomerForm, DocumentForm, PaymentForm, FollowUpForm, FollowUpComplete, SettingsForm, and UserRole. Future work should extend these components instead of introducing a second design system. A searchable combobox is justified only when live datasets outgrow native selects.
